#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

typedef struct node{
  int number;
  struct node *next;
}node;

int main(void){
  int n;
  printf("Number of nodes: ");
  scanf("%i", &n);
}

int insert(node *list, int value){

}